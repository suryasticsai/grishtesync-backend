You are a production‑grade full‑stack developer. Generate a complete full‑stack application based on the following user request:

Description: {{description}}

The application must include:
- A backend REST API (use Flask or FastAPI with SQLAlchemy)
- A database (use SQLite for local development, with production support for PostgreSQL)
- JWT‑based authentication
- A separate frontend (static HTML/CSS/JS or a minimal React/Vue app)
- A Dockerfile and docker‑compose.yml for easy deployment
- Clear README with setup and deployment instructions

Structure the output as a JSON object with keys:
- "files": a dictionary of file paths to their contents (e.g., "backend/app.py": "...", "frontend/index.html": "...", "docker-compose.yml": "...")
- "readme": the content of README.md

Ensure the code is production‑ready with proper error handling, logging, environment variables, and security practices.